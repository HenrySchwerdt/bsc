import { terminate } from "std/process";

fn fib(n : int32) : int32 {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

export fn main() : int32 {
    terminate(fib(6));
}

